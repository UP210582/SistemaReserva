import React from 'react';	
import PropTypes from 'prop-types';

const TextBox = (props) => {
    return(
        <div className='TextBox'>

        </div>
    );
}

TextBox.propTypes = {
    
}

export default TextBox;